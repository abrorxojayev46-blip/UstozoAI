{
  "buildCommand": "npm run build",
  "devCommand": "npm run web",
  "outputDirectory": "dist",
  "framework": "expo",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
